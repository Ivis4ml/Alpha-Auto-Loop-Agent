# Fable 构建任务书：Alpha-Auto-Loop-Agent（双市场版）

> 本文件是交付给 Fable（claude-fable-5）的构建任务书，目标是从零构建一个市场无关、大语言模型驱动的自动化研究与交易 Agent 应用，同时覆盖美股与中国 A 股。
>
> 本文件的编写原则：讲清楚要达成什么效果，讲清楚手头有哪些数据与工具背景，不规定具体的代码架构、目录结构、算法选型或模块命名。第 3 节写成"必须达成的效果"或"不可绕过的约束"的内容是真正的硬性要求；除此之外的一切设计决策（架构分层、搜索算法、检索技术选型、GUI 组件划分、模块与文件命名等）都由你判断，不必参照任何参考仓库的具体实现，第 4 节会把交还给你的自由度显式列出来。文中提到的四个参考仓库（minute_factor_agent、AlphaForge、crawler_cls、Alpha-Data）是背景资产与前车之鉴的来源，不是蓝图；引用它们时会说明这是"已验证值得延续的效果"、"一种实现选择你可以另选"，还是"一个应主动避免的坑"。

---

## 0. 定位与工作方式

你是这个项目的自主研究平台工程师。四个参考仓库与相关数据均在本机磁盘，动工前应实际读取相关源码与数据，不要只依赖本文件的转述。本文件基于一次多智能体调研写成，调研过程中发现参考仓库自己的 README 与实际实现之间存在若干处滞后与出入（具体见第 2、5 节），转述本身可能过时，一切以你亲自核实的磁盘现状为准。

工作纪律：

- 中文撰写文档与界面文案，采用学术论文与正式技术文档的文体，不使用 em dash，不使用非正式的网络流行语，代码中不使用表情符号。
- Git 走特性分支加 Pull Request，不直接提交 main。代码需符合所用语言的社区规范、带类型注解或类型提示、带异常处理，不留 TODO 占位。
- 第 3 节是效果层面的约束，不是实现层面的规定；第 4 节明确列出交还给你的自由度；第 5 节是调研中发现的具体陷阱，建议主动规避而不是重蹈覆辙；第 6 节是需要你或用户拍板、本文件无法替你决定的开放问题。

---

## 1. 产品目标

用任意标的（美股或 A 股）的分钟与日线行情，加上财联社新闻与情绪面（美股一侧可另加 Polymarket 宏观事件面），由大语言模型驱动一个尽量少人工干预的研究与决策 Agent。它需要做到：

- **自动化因子研究循环**：从数据审计到因子提出、样本外验证、治理门控、策略草拟、回测评估，循环产出经得起统计检验的候选，而不是产出好看但经不起复验的结果。
- **自动回测**：对候选策略在双市场上跑出可复现、含统计显著性检验与成本敏感性分析的回测结果。
- **交易计划（不含真实下单）**：对头部流动性标的给出选股与买卖计划，预留信号到订单的执行接口协议，当前阶段不接任何真实券商（第 3.6 节）。
- **新闻的实时检索与情绪融合**：自建新闻的抓取、检索与情绪打分能力，把新闻情绪面与量价因子融合进选股决策。
- **与用户的对话交互**：围绕某个标的与某段新闻，用户可以提问"是否相关""是利好还是利空"，系统检索证据后带引用作答。

系统同时服务美股与 A 股两个市场，两侧的分钟与日线行情数据均已就绪（第 2.1 节），不需要按市场分先后阶段实现。但两侧在结算规则、交易时段、复权可得性、下单合规环境上有实质差异，不能用同一套市场假设覆盖两者（第 3.2 节）。

---

## 2. 背景资产地图

### 2.1 双市场 MinuteDB（权威数据源，`Alpha-Data` 仓库）

两个市场的分钟与日线行情都已构建完成，物理位置均在 `/Users/xinyu/Code/Workspace/ncvx-project/Alpha-Data/data/`，产出与消费口径见该仓库的 `README.md`、`CLAUDE.md`、`docs/DATA_GUIDE.md`（尤其是 §6，专讲 A 股）。以下数字来自本次调研对磁盘现状的直接核实，不是转抄文档。

**美股（`data/equity/`，来源 massive.com，即 Polygon.io 更名后的品牌）**：2020-01-02 至 2026-06-29，1,630 个交易日；`minute/` 下 21,855 个标的目录，`listing.parquet` 收录 35,895 个标的（含退市，其中在市 12,900）；分钟 bar 约 25.9 亿行，日线约 1,671 万行，`corp_actions.parquet` 约 143 万行、覆盖 69,422 个标的；磁盘约 67GB（其中原始 Flat Files 30GB，文档说明构建后可删，实际尚未清理）。分钟表列：`symbol, ts（收盘时刻，America/New_York 本地）, open/high/low/close（float32，未复权）, volume（股）, trade_count, source, ingested_utc, raw_hash`，无 vwap、无 amount。日线表多一列 `amount`，由 RTH 390 根分钟 bar 的 `Σ(close×volume)` 聚合得出，是估算值，不是真实成交额。RTH 由分钟表自身含盘前盘后（04:01-20:00）、日线聚合阶段过滤 09:31-16:00 得到。

**A 股（`data/cn_equity/`，来源本地中证 1 分钟 CSV，2026 年 7 月构建完成）**：2020-01-02 至 2025-12-31，1,455 个交易日；5,710 个标的（据 `docs/DATA_GUIDE.md` §6 约 5,180 个股加 530 指数，含退市；交易所分布深交所 3,220、上交所 2,488、北交所 2）；分钟 bar 约 17.9 亿行，日线约 742.3 万行；磁盘实测约 27GB（`docs/DATA_GUIDE.md` 总览表写"约 17GB"，是最近一次新增 vwap/amount 列的提交之后未同步更新的滞后数字，请以 27GB 为准）。分钟表列在美股基础上多两列 `vwap`（=amount/volume，真实分钟 VWAP）与 `amount`（源"成交额"原样保留，真实成交额，单位元）；`ts` 为**北京本地墙钟时间**，不是 UTC 也不是美东；`volume` 单位为**手（1 手 = 100 股）**，指数记 0；`trade_count` 恒为 0（源无此字段）；分钟与日线**均不做 RTH 过滤**，09:30 集合竞价与 15:00 收盘集合竞价都计入。

两个市场共用同一套目录布局模板（`minute/{symbol}/{year}.parquet`、`daily/{symbol}.parquet`、`corp_actions.parquet`、`listing.parquet`），可以用同一套读取逻辑访问。这是这批数据对新 app 最大的便利：双市场从第一天起就能用同一条读取路径接入，不需要先做美股再排 A 股。但口径差异是实质性的，不能假设两边字段语义相同：

| 差异项 | 美股 | A 股 |
| --- | --- | --- |
| 时区与墙钟含义 | bar 收盘时刻，已转为 America/New_York | 源时间戳原样保留，北京本地墙钟 |
| RTH 过滤 | 分钟表含盘前盘后，日线聚合按 RTH 过滤 | 分钟与日线均不过滤，全时段（含集合竞价）计入 |
| 成交量单位 | 股 | 手（1 手 = 100 股） |
| amount 语义 | 日线级估算值（`Σ close×volume`），分钟级不存在 | 分钟与日线均为源直接保留的真实成交额 |
| 复权因子 | 可得（`corp_actions` 约 143 万行） | **不可得**（`corp_actions` 为空表，源不含拆股分红） |
| trade_count | 可得（源 `transactions` 列） | 不可得，恒为 0 |
| symbol 归一 | 大写原生 ticker，类别股/权证的点号后缀保留 | `.SH`/`.SZ`/`.BJ` 后缀，保留前导零 |

Alpha-Data 自带一些不依赖任何下游框架、可以直接调用的读取工具：`alpha_data/common/calendar.py`（交易日历与分钟网格，基于 `pandas_market_calendars`）、`alpha_data/polymarket/`（见 2.2 节）。文档推荐的正式读取封装是 AlphaForge 的 `alphaforge.providers.db.minute_db.MinuteDB` 与 `DbMinuteSource`，但这是另一个仓库的代码，需要额外安装；不引入这层封装的话，用 DuckDB 直接对 Parquet 做 glob 查询是唯一不需要任何额外安装、当场可用的路径，具体写法见 `docs/DATA_GUIDE.md` §3.4/§5.3/§6.2。是否引入 AlphaForge 的读取封装、还是自己写一套读取层，由你决定，但引入前请先看第 5 节第一条：那套封装在 A 股日线场景下有一个会静默产生错误数据的缺陷。

### 2.2 Polymarket 宏观面（美股）

美股一侧另有 Polymarket 预测市场数据作为宏观与事件面的另类数据，位于 `data/polymarket/`，来源 HuggingFace 数据集 `TimeSeventeen/Polymarket-v1`（CC-BY-4.0）。`daily_aligned` 层覆盖 2022-11-21 至 2026-04-28；已加工出的特征宽表 `data/polymarket/features/market_features.parquet` 有 284,741 行、47 列，按 `(trade_date, minute)` 归并到美股分钟网格，覆盖五个宏观主题（联邦利率、衰退、通胀、总统选举、政府停摆）各 9 个特征，构建时已做无前视处理（特征只用严格早于该分钟的成交，解析后置空）。这层数据磁盘约 46GB，读取工具在 `alpha_data/polymarket/`（`store`/`catalog`/`features` 三个模块，不依赖 AlphaForge，可直接调用）。这是美股独有的另类数据，A 股没有对应物，是否使用、如何与新闻情绪面并列，由你判断。

### 2.3 新闻语料与舆情处理现状（财联社）

财联社电报是计划接入的第一版新闻源，已经积累了两层资产，成熟度不同，不要混为一谈。

**爬虫本身**（`ncvx-project/分钟线策略任务-B/Crawler/crawler_cls`）是相对成熟的资产：直连 `https://www.cls.cn/v1/roll/get_roll_list`，签名算法与限速机制清晰（`src/cls_api.py`），历史回填与实时轮询（`src/cls_realtime.py`，默认 30 秒一轮）都有实现。本机已有回填语料，经精确统计为 2020-01-01 至 2026-06-18 共 2,361 个按日 CSV、894,220 条记录、约 474MB，时间上无缺口，但**最新数据落后当前日期约两周**，说明近期没有运行采集。现有代码从抓取到写入磁盘的过程中会永久丢弃原始文章 id（去重靠"时间加正文"元组而非 id）以及部分标签字段的结构信息；仓库内没有找到任何服务条款或合规声明，这是需要向用户明确的空白，不要默认已经获得授权（第 6 节列为待拍板项）。若要获取比现有 CSV 更丰富的字段（比如某条电报关联哪些个股），需要自己重新对接原始接口去核实服务端实际返回什么字段，不要假设现成代码已经拿到了这些信息。

**新闻的检索与问答能力基本不存在，需要从零设计**，这一点需要向你澄清：参考 app `minute_factor_agent` 的新闻模块（`mfa/crawler/` 下的 `cls.py`、`sentiment_factors.py`、`news_digest.py`）不是一个基于向量检索的 RAG 系统。全仓库没有 embedding、没有向量库、没有语义相似度检索，依赖清单里也没有相关的库。它的实际做法是：按日期做文件级过滤，对区间内全部文本用一个只覆盖三个题材（半导体、光模块、算力）、每题材 4 到 6 个关键词的中文关键词词典做子串匹配和情绪打分，再做结构化聚合，可选地把聚合后的统计数字（不是新闻原文片段）交给大模型改写成一段自然语言摘要。这个摘要面向整个市场与整个日期区间一次性生成，不按个股过滤，不支持追问。也没有任何针对个股的"是否相关、利好利空"问答实现：唯一的对话式组件是解释系统内部术语的助教机器人，不接触新闻语料。个股与题材的映射关系在这个项目自己的 `docs/DATA_GAPS.md` 里也被列为缺口，尚不存在。

因此，新闻检索与对话问答（第 3.4 节）是这个新 app 相对已有资产的真正增量，需要设计真正的检索能力与情绪打分方法，而不是把 `news_digest.py` 移植过来。值得继承的是财联社语料本身、增量补抓策略（`freshness.py` 的覆盖比对加尾部优先补抓），以及下一节提到的几个通用工程模式。

### 2.4 minute_factor_agent 的其余工程资产

除新闻模块外，`minute_factor_agent`（`ncvx-project/分钟线策略任务-B/minute_factor_agent`）还有两个和具体业务无关、值得参考的通用工程模式：

- **事件总线**（`mfa/agent/events.py`）：线程局部单一 sink，只追加写 JSON Lines 文件，事件种类收敛为固定小枚举（phase/think/tool/result/gate/artifact/info/warn），写入失败静默吞掉不打断主流程，消费侧按序号增量读取，支持运行中轮询与运行后回放。复杂度刻意压低，适合单进程多线程场景。
- **工具即纯函数**（`mfa/agent/toolkit.py`）：研究相关的操作是一组无状态函数（输入 pandas 对象与标量参数，输出可直接序列化的 dict、Series、float），流水线、单票 ReAct、并行 Swarm 三个入口共用同一批函数，避免评估口径在不同入口间逐渐分叉。
- **LLM 调用的双轨缓存**（`mfa/agent/llm.py`）：厂商无关的补全接口（默认走本机 `claude -p`，可切换到其他厂商或纯离线），explore、replay、auto 三态使调用可以离线确定性回放。

这三点是模式层面的经验，与新闻或 A 股无关，可以脱离具体领域直接借鉴思路，不必照抄接口签名。

### 2.5 AlphaForge 参考架构

AlphaForge（`ncvx-project/分钟线策略任务-B/AlphaForge`）是把 `minute_factor_agent` 迁移为市场无关架构、并补齐美股与期权支持的已完成案例，带测试（import-linter 分析该仓库约 197 个文件、904 个依赖关系，规模可观）。它不是新 app 的蓝图，但里面有几项经过代码、测试与本次调研自己补充验证确认过的经验，分两类。

**已验证、值得作为效果延续的约束**（不是必须照抄的实现）：

1. 纯函数研究底座与有副作用运行时之间存在一条机械可校验的边界，并且覆盖到最核心的计算层。AlphaForge 用 import-linter 做到了这一点，但**只覆盖了三条底层契约**（`core` 不依赖任何上层或 subprocess/requests/socket；`market` 只依赖 `core`；`options` 只依赖 `core` 加 `market`，配置在 `pyproject.toml [tool.importlinter]`），更上层的 `providers`、`kernel`、`apps` 之间目前只是约定，代码里已经出现两处真实绕过静态检查的反向依赖（字符串动态 import、函数内延迟 import）。如果你打算用类似机制，建议覆盖全部层级，不要只做半截。
2. 存在一个显式、持久化、跨调用可查询的"是否已消费"记录，重复触发要被拒绝而不是静默放行。AlphaForge 的实现在隔离单测下正确，但黑盒重跑同一条流水线命令未必稳定触发保护，大概率与并行 worker 完成顺序影响哈希键有关，这类键的构成需要专门验证并发下的稳定性。
3. 存在一个第三方无法伪造、字段被篡改即失效的权限凭证机制，三种伪造尝试（异源密钥签发、篡改字段、手工构造）均被正确拒绝。
4. 存在一个能检测"同一逻辑键对应的数据发生变化"并拒绝静默覆盖的缓存层，口径变更检测与只读回放模式均验证正确，但仓库本身没有任何自动化测试覆盖这一层，延续这个要求时应当把测试一起补上。
5. 面向用户或模型的默认文案禁止硬编码某一个具体市场的身份或该市场特有的规则。这是 AlphaForge 自己踩过的坑：核心运行时里至今仍有四处系统提示词写着"你是 A 股分钟线研究员"，其中一处还连带写死了 T+1 结算假设（与当前默认市场美股的 T+0 矛盾），还有一处硬编码 6 位 A 股代码作为默认股票池。这些残留出现在按设计应当"市场无关"的层里，说明"写代码时留意"本身并不可靠，建议做一条自动化检查而不是只靠人工审查。
6. 第六条与前五条的证据性质不同：前五条是"AlphaForge 这里做到了，值得延续"，这一条是"AlphaForge 这里没做到，新项目要引以为戒"。像 Skills、MCP 这类最能体现平台化能力的新增子系统，测试覆盖需要和核心研究逻辑同等级别，不能满足于"数量对了"这种冒烟级验证。AlphaForge 的 Skills 只有一条注册数量测试，MCP 完全没有测试，而且"host 真正连接一个外部 MCP server"这部分代码从未实现或验证过，尽管文档把它称为一等公民。

**只是实现选择，交还给你自由发挥**：多因子组合搜索用的具体算法（AlphaForge 选的是强化学习式组内比较加多目标非支配排序，你可以用网格搜索、贝叶斯优化、进化算法或别的任何方法，只要对多目标与 LLM 调用样本效率负责）；"拆成若干并行视角、再交叉复核"这个结构性思路（AlphaForge 里独立出现了两次，提案阶段与复核阶段的视角命名完全不同，数量本身没有原则性依据，按自己的任务重新拆解即可）；具体的目录树命名与因子表达式系统的算子集合（生成的表达式必须限定在白名单上求值、不允许任意代码执行、不允许触及未来数据，这条安全原则要保留，具体算子按你的数据形态设计）；GUI 的具体视图与组件划分（后端产物与前端视图松耦合、新增产物类型不必重写框架，这个模式值得延续，具体的十几个视图名字对新 app 没有直接复用价值）；第 2 条"一次性消费记录"的具体键由哪些字段组成、第 4 点提到的 LLM 调用缓存具体分几种状态，这类实现细节本身也是有讲究的（第 2 条已经提示了并发稳定性问题），交还给你根据自己的运行时设计。

补充两点容易被文档误导的地方：AlphaForge README 所称的"零构建、零依赖"只是指服务端用语言标准库做静态文件托管，不需要额外的后端框架依赖；前端（Vite、React、TypeScript、ECharts，约 1.3 万行）仍然需要正常的构建步骤才能得到可用产物。它的桌面壳体积很大（安装包 237MB，引擎解包 410MB），这是"把 Python 打包成不依赖用户本机环境的桌面应用"这一工程决策的必然代价，不是 GUI 技术选型的问题；Vite、React、TypeScript、ECharts 这套组合本身成熟、主流、上手成本低，值得直接沿用而不必重新评估。

### 2.6 平台与外部背景能力

当前（2026 年 7 月）可以确认是官方现存能力、不是路线图的几项 Claude 平台机制，构建时可以放心使用：Agent Skills（把操作说明与脚本打包为技能包，渐进式加载）、MCP 的 host 与 server 划分（可以作为消费方接入外部数据源，也可以反过来对外暴露能力）、多 subagent 并行编排、生命周期各节点的 hooks、分级权限与沙盒执行、面向长时间运行场景的会话保存恢复与托管运行能力。这些是可以用来构建长时间自主运行的研究型 Agent 应用的现成组件，具体怎么组合由你决定。

本环境已连接 Alpha Vantage MCP（大量行情、基本面、技术指标、`NEWS_SENTIMENT` 等端点）与 Wolfram MCP（可用于期权定价等计算的交叉校验），可以作为外部数据源或计算校验的补充，尤其是 Alpha Vantage 的英文新闻情绪端点可以作为美股一侧新闻源的补充或对照。

金融新闻 RAG 领域公开资料里常见的检索架构大致分纯向量语义检索、关键词加结构化字段过滤、混合检索（向量与关键词并用加重排序，公开评测显示相对纯向量检索有约 20% 到 35% 的召回提升）几类；point-in-time 正确性是量化金融里的成熟概念，近两年也被明确延伸到基于 LLM 的金融 Agent 与 RAG 评测中，新闻类数据源尤其容易因为新闻更正、事后关联报道、模型预训练语料本身携带晚于查询时点的信息而引入前视偏差。这些是背景参考，不构成指定架构，具体技术选型由你判断。

---

## 3. 不可绕过的要求

### 3.1 科研诚信治理

这是整个项目的价值内核，来自对项目最初研究提案与技术方案原文的直接通读。以下八条是必须达成的效果，具体怎么实现由你决定：

1. 系统必须同时具备"信号存在时能找到"与"信号不存在时不虚报"两种能力，后者要用可测量的误报率证明，不能只靠自我声明谨慎。这两种能力必须配对验证，缺一不构成诚实性的证据：一个无论输入什么都报告"未发现"的系统同样能得到很低的误报率，但那只是迟钝而不是诚实；真正的诚实要求系统在已知强度的注入信号上，发现率随信号强度单调上升，同时在无信号对照上保持低误报率。要让这个误报率可信，注入的信号强度、随机种子等参数需要对研究过程本身隐藏（例如评估环节与生成候选的环节相互隔离），否则"可测量"这件事本身可能被绕过。一个在不存在可发现 alpha 的设定里正确报告"未发现"的系统，比一个在该设定里也报告"发现了"的系统更可信，这是设计上要显式追求的结果，不是失败模式。
2. 任何有效性结论的可信度，取决于产生该结论所依赖的全部规则（不只是模型参数，还包括提示版本、知识快照、因子表达式的语法、预筛模板、统计门控阈值、成本模型、股票池定义、失败归因规则与奖励回写规则）是否在进入验证阶段之前就已固定；固定之后的任何改动都使相应阶段的结果失去被采信为样本外的资格。
3. 判断一段数据能否作为有效样本外，标准是"冻结前是否被任何一次运行触碰过"，不是"日历上是否晚于训练区间"。已经过去的时间不会因为看起来晚就自动获得验证效力，构建时的当前日期是 2026-07-02，截至这个日期之前的任何区间都已经是历史，能否作为样本外证据取决于它是否在冻结点之前从未被使用过。
4. 系统必须能够说清楚自己总共产生过多少候选方案，覆盖每一次迭代、每一版提示词、每一次知识更新，任何显著性结论都必须针对这个完整数目做统计修正。哪怕从未让语言模型看着分数调参数，只要研究循环本身存在"失败了就调整策略再来一轮"的结构，这个迭代过程本身就已经消耗统计意义上的自由度，必须计入这个数目。
5. 借助大语言模型提出改进时，系统要能区分"仅凭先验知识与经济逻辑推理、验证只做一次"和"反复参照验证结果引导下一次尝试"这两种模式，并证明自己走的是前者；如果确实使用训练反馈，要能证明奖励不是单独优化一个以噪声主导的统计量，且最终仍然要过第 4 条的紧缩门控，否则等同带反馈的重复检验滥用。
6. "使用了智能组件后结果更好"不能直接当作"智能组件本身更强"的证据。后一种更有分量的主张，只有在因子表达式的语法、数据字段范围、候选预算、算力与 token 与回测预算、统计门控标准、训练验证样本外切分这六个维度都完全相同的对照下（相对不使用智能组件的随机或模板搜索、人工因子集，以及逐项去除知识库、评审、失败记忆等组件的消融）测得的差值才能成立；六个维度里任何一个不一致，测得的差异都可能来自预算更大或门槛更松，而不是来自智能组件本身。
7. 事后可能被重新认定的数据只能向前使用，不能用当前已知的版本回填历史。这条对两类数据的适用方式不同：题材与板块归属这类结构化标签，风险在于成分被事后追加重排，除非能证明历史版本确实是当时的原貌，否则不得进入训练与样本外评估，只能从有把握的快照起点开始向前积累；新闻文本类数据，风险更多在于发布时间与市场实际可见时间的时间戳错位，系统要能区分这两个时间戳，只用截至查询时点已可见的记录。
8. 收益结论必须注明是否已扣除成本。未扣除成本的收益数字可以作为阶段性、比较性的展示，但不能单独作为某个因子或策略最终有效的证据；成本后净收益门控与换手惩罚要作为准入的一部分保留在管线里，不能因为展示方便就从管线中去掉。

以上八条之外还有三点原始文档明确论证过的细节：策略生成与策略评审不应该是同一个模型角色，需要某种机制避免自证；未来函数检查、字段合法性、参数边界一类的关键判定，应该交给不依赖语言模型的确定性规则引擎完成，不能把这类判定的自由裁量权交给可能被诱导的生成模型本身；任何参数变化都应该产生新版本而不是覆盖旧版本，保证一次失败的尝试不会被悄悄抹去重来。

关于第 5 条还需要补充一点背景：原始方法论文档对"训练阶段能否借助回测反馈生成候选"这个问题本身存在两种未完全统一的表述，一种设想允许在多重限制下使用回测反馈训练奖励模型，另一种明确要求完全不看回测结果、只改一次。文档自己把这两种表述之间的取舍标注为需要人来拍板的问题（第 6 节列为开放决策之一），不是本文件替你做出的选择。

### 3.2 市场无关

研究、回测、因子、治理相关的核心逻辑不应该出现按市场分支的判断；市场之间的差异（结算规则、交易时段、涨跌停或熔断这类交易制度、复权可得性、下单合规环境）应该收敛成一套可插拔的市场描述，新增一个市场只需要新增一份市场描述，不需要改动核心逻辑。这不是审美要求，是 AlphaForge 自己在这件事上吃过亏之后确认有必要的：它的核心研究运行时里至今仍有四处系统提示词写死"你是 A 股研究员"、一处硬编码 A 股默认股票池（见 2.5 节），这些代码原本被设计成市场无关的层，说明这类耦合不会因为架构分层就自动避免，需要显式设计与检查手段。面向大语言模型或用户的任何文案、任何默认值，都不应该假设一个具体市场。

### 3.3 双市场数据接入的具体处理要求

两个市场的 MinuteDB 都已就绪（2.1 节），接入时至少要正确处理以下几点：

- 不能假设两个市场的 `ts` 字段语义相同，也不能假设两个市场的 `volume` 单位相同。
- A 股没有复权因子，跨除权日的收益不能直接使用未复权价格计算；是否现在就补一份复权数据源、还是先在明确标注的前提下用未复权价格研究，是第 6 节的一个待拍板项。
- 如果引入 AlphaForge 的读取封装，要注意它在日线粒度补算 `amount` 时，找不到 vwap 列就会用 `close×volume` 无条件覆盖已经算好的真实值，这对 A 股日线会把真实成交额压低约 100 倍（第 5 节第一条有完整依据）。不引入这层封装，或者引入后先修好这个问题，由你决定。
- A 股一侧构建这批数据的代码目前在 Alpha-Data 仓库一个尚未合并的功能分支上，该仓库的 `main` 分支与 README 都还没有反映这层数据的存在。开始使用前请先确认这批数据在你实际读取的路径下是可用的，不要因为看了 `main` 分支或 README 就误以为这层数据不存在。
- 两个市场目前都没有可用的指数成分表或基本面数据，这不是 Alpha-Data 文档里标注的缺口，而是文档里完全没有涉及的空白，本次调研专门检索确认过。构造头部大盘股票池时，两边只能先用逐日按成交额取 top-N 近似，是否接入外部成分表是第 6 节的一个待拍板项。

### 3.4 新闻检索、情绪融合与对话问答

这是新 app 相对已有资产的主要增量（2.3 节），需要从零设计：

- 需要一个能把新闻与具体标的关联起来的机制（实体链接、别名映射，或别的合适方法），这个映射关系目前在任何参考资产里都不存在。
- 需要真正意义上的检索能力，能针对一次具体查询召回最相关的新闻子集，而不是像现有资产那样对整个区间做一次性聚合摘要。检索技术选型不做规定。
- 检索与生成都必须遵守 point-in-time：回答历史问题时只能使用截至该时点已经可见的新闻，新闻的发布时间与市场实际可见时间如果存在错配，要按后者过滤，不能用抓取或整理入库的时间戳代替。
- 需要支持对话式交互：用户输入标的与问题，系统检索证据后作答，回答应当附带依据的新闻时间戳与来源，不足以支撑判断时应如实说明信息不足。头部大盘股大模型常识本身相对充分，可以结合常识作答，但要清楚标注哪部分是新闻证据、哪部分是模型背景知识。
- 情绪打分方法不做规定，但如果参考现有的中文关键词词典思路，要知道它目前只覆盖三个题材、每题材只有 4 到 6 个关键词，项目自己也承认只是起步，不构成一个可信的起点，需要重新设计或大幅扩充验证。
- 这一部分的产出如果被实际用于选股决策（第 3.5、3.6 节），对应的检索与判断过程要能被记录追溯；如果只是交互式问答、不影响选股决策，不需要按第 3.1 节的要求纳入试验计数。
- 美股一侧的新闻源不必只依赖财联社，可以用 Alpha Vantage 的 `NEWS_SENTIMENT` 之类的英文源，或别的合适来源；财联社爬虫目前没有发现任何服务条款或合规声明（2.3 节），接入前是否需要专门核实，是第 6 节的一个待拍板项。

### 3.5 自动化研究循环与自动回测

系统需要能够自主完成从数据审计到因子发现、样本外验证、治理门控、策略草拟、回测评估的循环，并把 3.1 节的统计要求结构化地嵌入这个循环，而不是事后补一层检查。回测需要在双市场上都可运行，产出可复现、包含统计显著性检验与至少一种成本敏感性分析的结果。循环的具体拓扑不做规定，只要满足 3.1 节列出的效果性约束。

研究循环的每次运行必须可追溯、可复现：给定同一份记录下来的运行配置（随机种子、数据版本、口径版本、所用的提示版本与模型版本），重新运行应当得到相同的结果，包括大语言模型产生的内容，这意味着大语言模型调用需要某种可关联到具体运行记录的缓存或版本化机制，不能因为模型的非确定性输出而让同一个历史结论无法复核。是否要求整条流水线在没有网络或 API key 时也能完整跑通，由你判断这是否值得作为一个工程目标，但如果实现，离线路径产出的结果也要满足前面这条可复现要求。

### 3.6 交易计划与预留执行接口

自动交易在这个项目里的范围已经明确：系统负责产出结构化的交易计划（标的、方向、建议买点、卖出框架、成本后净收益与容量估计），不在当前阶段连接任何真实券商的下单接口。但交易计划和执行之间需要有一层清晰的信号到订单执行接口协议，使得未来如果要接入真实或模拟执行，不需要改动研究与决策层的代码。这层接口协议具体怎么设计由你决定，当前阶段只需要提供一个把交易计划转成结构化订单意图并记录下来的实现，不需要真的对接任何外部系统。

背景参考，不构成指定方案：美股主流经纪商里，Alpaca 提供 REST 与 WebSocket 接入，并明确提供与实盘同一套 API 规范的免费模拟盘沙盒，只需更换密钥与接口地址即可切换，分阶段验证比较方便；Interactive Brokers 提供 TWS API、Client Portal Web API 与面向机构和高频场景的 FIX 协议接入，同时面向个人与专业交易者，具体的模拟盘沙盒安排需要另行核实。中国大陆自 2024 年 10 月 8 日起施行《证券市场程序化交易管理规定（试行）》，核心要求是先报告、后交易，个人投资者可用的合规程序化下单渠道相对有限，规模化的量化机构通常经由券商专用通道接入。也就是说，即便未来决定扩展到真实或模拟执行，美股与 A 股不是对称的两个选项，A 股一侧需要单独评估合规路径，设计接口协议时应当预留这种不对称性，不要假设两边可以套用同一个执行适配器。

### 3.7 产品体验

界面观感对标 Codex 这类 Mac 原生应用的美观与克制，配色和谐；交互上既要有能看到 Agent 在做什么的视角（思考过程、工具调用、进度），也要有能直接对话的入口；启动与响应要快，能并行的地方并行，不要让用户长时间对着空白等待而不知道系统在做什么。

---

## 4. 交还给 Fable 的设计自由度

为避免误解，把不做规定的部分明确列出来，以下这些完全由你决定，参考仓库的具体做法仅供参考，不代表更优：

- 整体代码架构、分层方式、目录与模块命名。
- 因子研究与组合搜索使用的具体算法。
- 因子表达式系统的具体设计，只要满足"生成的表达式在白名单上求值、不允许任意代码执行、不允许触及未来数据"这条安全要求。
- 是否采用多个并行视角提案再交叉复核的结构，如果采用，视角的数量与命名。
- 新闻检索的具体技术栈，是否使用向量数据库、使用哪个 embedding 模型、检索前后是否做重排。
- GUI 的具体视图划分、组件粒度、状态管理方式，后端产物与前端如何通信。
- 实现语言与技术栈的选择。参考仓库与 Alpha-Data 的整个数据生态是 Python（DuckDB、pandas、pyarrow），换一种语言意味着需要重新实现数据读取层，这是一个真实的成本，值得纳入判断，但不是强制要求继续用 Python。
- 事件、日志、缓存、配置这类基础设施的具体设计。

判断标准是：第 3 节列出的每一条效果，你打算怎么达成都可以；第 3 节没有提到的任何东西，都不需要参照任何参考仓库。

---

## 5. 已知陷阱清单

以下是本次调研中亲自核实过的具体问题，建议主动规避：

1. **AlphaForge 的日线读取封装会静默错算 A 股成交额**：`DbMinuteSource` 在补算日线 `amount` 时，找不到 vwap 列就用 `close×volume` 无条件覆盖已经算好的真实值；A 股日线 Parquet 本身没有 vwap 列（只有分钟级才有），经这条路径读出的 A 股日线 amount 会因为"手"与"股"的单位差被压低约 100 倍。分钟级 `read_minute` 路径不受影响。
2. **A 股没有复权因子**：`corp_actions.parquet` 为空表，源数据本身不含拆股分红，跨除权日的收益不可直接用未复权价格计算。
3. **A 股与美股的 `corp_actions` 表字段类型不一致**：A 股那张空表的字段类型是整数，美股对应字段是字符串与浮点数，如果按第 3.2 节"市场无关"的要求把两张表按同一套逻辑联合处理，会因为字段类型不一致而报错，需要在读取层显式把两边的字段类型统一起来。
4. **A 股数据管线在未合并的功能分支上**：Alpha-Data 的 `main` 分支与 README 都不反映这层数据的存在，容易被误判为"还没做完"。
5. **A 股原始 CSV 源目录已不在本机**：若要扩展 2026 年及以后的数据，需要先重新准备原始数据源。
6. **A 股的 RTH 参数目前是复用美股的时段谓词**：`rth_only=True` 实际效果只是剔除 09:30 那一根，不是真正按 A 股交易时段做的过滤，不要假设它已经正确处理了 A 股的时段结构。
7. **AlphaForge 的分层校验只覆盖了三层**：`providers`、`kernel`、`apps` 之间已经出现两处真实绕开静态检查的反向依赖。
8. **AlphaForge 的一次性消费键对并发调度顺序敏感**：黑盒重跑同一条流水线命令未必稳定触发"重复消费"保护。
9. **AlphaForge 的 Skills 与 MCP 测试覆盖是全仓库最薄弱的部分**："host 连接真实外部 MCP server"这部分能力从未被真正实现或验证过。
10. **在治理门控与试验登记机制就位之前引入更强的候选生成能力，会放大而不是缓解过拟合风险**：生成越强，在干净的统计约束建立之前越容易钻空子，正确的顺序是先把不依赖大语言模型的基线与门控跑通，再引入语言模型生成，不要反过来。
11. **minute_factor_agent 的新闻模块不是向量检索式 RAG**：不能直接当作新闻问答的基座，检索能力与个股映射关系都要从零设计。
12. **财联社接口存在约三成、且无法回填的历史覆盖率上限**：`get_roll_list` 只返回服务端标记为"在榜"的电报，一部分电报发布后会被移出这个标记、历史查询就再也查不到，因此历史回填条数大约只有事发当时实时采集条数的七成，且这部分缺口无法事后补回。这与"最近两周未更新"是两个不同性质的问题：后者补采就能解决，前者补采也解决不了，评估新闻类因子的样本完整性时需要把这个结构性上限纳入考虑。
13. **财联社爬虫的限速是进程内令牌桶，不是跨进程全局限速**：历史回填与实时采集若同时作为两个进程运行，合计请求频率可能达到配置值的两倍；仓库内也没有发现任何服务条款或合规声明，这是需要明确的空白，不是默认已获授权的信号。

---

## 6. 待拍板的开放决策

以下问题本文件无法替你或用户决定，建议在动工前或遇到时明确：

1. 美股与 A 股的头部大盘选池口径：两边都没有指数成分表，是否接受逐日按成交额取 top-N 近似（N 取多少），还是投入资源引入外部真实成分表。
2. A 股复权因子：是否现在就从外部免费源补一份拆股分红数据重建 `corp_actions`，还是先在明确标注缺口的前提下用未复权价格研究。
3. Alpha-Data 里 A 股数据管线所在的功能分支尚未合并：是先在 Alpha-Data 侧发起合并并更新其 README，还是新 app 直接指向该分支读取。
4. 财联社爬虫未发现任何服务条款或合规声明，是否需要先做一次专门核实，还是接受现状继续接入。
5. 财联社历史语料已经两周未更新，是否现在就恢复实时采集或补齐回填，长期是否要一直挂着实时采集进程。
6. 新闻情绪打分方法：纯规则词典，还是词典加大语言模型双轨（后者成本更高但更准，且需要把大语言模型的判断标记为模型回算值，与真实历史区分开）。
7. 大语言模型的默认模型与预算基调：生成、评审、对话问答各用哪一档模型，以及是否长期允许联网调用而非只用可离线回放的缓存。
8. 交易执行接口（当前阶段不接真实券商，见 3.6 节）未来的扩展方向：美股一侧优先接 Alpaca 还是 Interactive Brokers；A 股一侧考虑到监管环境，是否直接放弃真实执行方向、只做研究与计划输出。
9. 是否把期权层纳入这一阶段的范围（参考仓库把它列为最后、范围最小的可选项）。
10. 研究循环在训练阶段能不能借助回测反馈来指导下一轮候选生成：原始方法论文档对这一点存在两种未完全统一的表述，一种设想在多重限制下允许，另一种明确要求完全不看回测结果、只改一次，文档自己把这个取舍标注为需要人来拍板的问题（第 3.1 节第 5 条相关说明），不是本文件替你选定的答案。

---

## 7. 验收标准与交付方式

不论具体怎么设计实现，以下几点最终都需要能够被验证：

1. 同一套研究与回测逻辑同时服务美股与 A 股，不出现按市场分支的判断；新增一个市场只需要新增市场层面的描述，不改动核心逻辑。
2. 第 3.1 节的八条效果性约束都能找到对应的实现与验证方式，不要求逐条对应一个模块，但要能说清楚每一条是怎么达成的。
3. 能够针对某个标的与某段新闻，通过对话得到带证据时间戳的回答，信息不足时如实说明，不用未来信息回答历史问题。
4. 能够产出双市场的交易计划，预留了信号到订单的执行接口协议，但当前不连接任何真实券商。
5. 面对不存在可发现信号的设定，系统能够正确报告"未发现"而不是制造虚假信号；所有用估计或代理值替代真实数据的地方都有清楚的标注，不静默处理。
6. 给定同一份运行配置，结果可以复现。

动工前建议你先给出一份自己的实施计划，写清楚打算如何验证第 3 节与本节列出的每一条，以及打算怎么拆分里程碑；这份计划的组织方式不需要对应本文件的章节顺序，本文件之外的一切设计判断都以你的专业判断为准。

---

（本任务书基于对 minute_factor_agent、AlphaForge、crawler_cls、Alpha-Data 四个仓库与项目原始方法论文档的一次多智能体只读调研写成。动工前请复核各仓库当前状态，本文件的数字与结论以磁盘实际为准。）

## 修订说明（2026-07-02）

相对此前版本的主要改动：确认 A 股 minute_db 已在 Alpha-Data 完整构建（此前认知是仍在远程服务器下载中，第 2.1 节与第 5 节第 4、5 条说明了这个认知过时的具体证据），据此把"美股先行、A 股并行接线"的分阶段叙事改为双市场并重；把此前对新闻模块的"自带实时 RAG 系统"表述改为如实说明现状（第 2.3 节），检索与问答能力需要从零设计；整体把大量实现层面的规定（目录树、具体算法名称、六视角命名、因子算子集合、GUI 组件树）改写为效果层面的要求，把架构与算法选型交还给 Fable；补充了一份基于本次调研新发现的具体陷阱清单（第 5 节），其中包含一个会导致 A 股日线成交额被静默错算的现实缺陷；交易执行范围收敛为"预留信号到订单的执行接口协议，当前阶段不接入真实券商"，并补充了美股与中国 A 股在券商接入合规环境上的不对称性。
